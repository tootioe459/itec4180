<?php
declare(strict_types=1);

require_once dirname(__DIR__, 2) . '/src/bootstrap.php';

requireLogin();

$repository = new ChatRepository($pdo);
$messages = $repository->getRecentMessages(50);

Response::json([
    'messages' => $messages,
]);
